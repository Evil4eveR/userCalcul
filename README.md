# userCalcul
calcul 2 number, only access to username 

download the calcUser.ccp file, then compile using `g++calcUser.cpp -o calcUser`, then to run it do `./calcUser` (the -o function is a way to name the file(thats why the file is named "calcUser")
